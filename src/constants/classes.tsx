const common = "p-1";

export const defaultAppClassName = `flex flex-col justify-between h-full ${common}}`;
export const defaultHeaderClassName =
  "flex items-center justify-between h-14 w-full p-4";
export const defaultNavigationButtonClassName = `rounded m-2 flex flex-row justify-center items-center`;
export const defaultFooterClassName = `text-sm ${common}`;
